int CallBlynk(){
  
  //Blynk.run();                                      //in die main????
  //Blynk.virtualWrite(V0, i32RetMittelwert);         //schreibe den Wert an die Handyapp

// This function will be called every time Slider Widget
// in Blynk app writes values to the Virtual Pin 1

}

/*
BLYNK_WRITE(V10){
  int pinValue = param.asInt(); // assigning incoming value from pin V1 to a variable
  // You can also use:
  // String i = param.asStr();
  // double d = param.asDouble();
  Serial.print("V10 Slider value is: ");
  Serial.println(pinValue);
}

BLYNK_WRITE(V11){
  int pinValue = param.asInt(); // assigning incoming value from pin V1 to a variable
  // You can also use:
  // String i = param.asStr();
  // double d = param.asDouble();
  Serial.print("V11 Slider value is: ");
  Serial.println(pinValue);
}

BLYNK_WRITE(V12){
  int pinValue = param.asInt(); // assigning incoming value from pin V1 to a variable
  // You can also use:
  // String i = param.asStr();
  // double d = param.asDouble();
  Serial.print("V12 Slider value is: ");
  Serial.println(pinValue);
}

BLYNK_WRITE(V13){
  int pinValue = param.asInt(); // assigning incoming value from pin V1 to a variable
  // You can also use:
  // String i = param.asStr();
  // double d = param.asDouble();
  Serial.print("V13 Slider value is: ");
  Serial.println(pinValue);
}
*/
